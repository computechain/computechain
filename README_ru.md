# ComputeChain

**ComputeChain** — это экспериментальный Layer-1 блокчейн, построенный на новой модели консенсуса и стимулов под названием **Proof-of-Compute (PoC)**, ориентированной на выполнение *полезных* вычислений на GPU (нацеленность на RTX 4090/5090 и новее).

В настоящее время сеть работает на стадии **Stage 4: Proof-of-Compute Framework**, обеспечивая стабильный многовалидаторный консенсус PoA, архитектуру подписей, готовую к постквантовой (PQ) эре, и экономическую модель на основе Gas.

---

## ✨ Ключевые особенности

### 🔐 **Консенсус и Безопасность**

* **Multi-Validator PoA (Round-Robin)**
* **Post-Quantum Архитектура подписей** (готовность к Dilithium/Falcon)
* **Детерминированное создание блоков**
* **Ротация валидаторов каждые 10 блоков (эпоха)**

### 💸 **Экономика и Стейт**

* **Модель Gas (как в Ethereum)** для защиты от спама
* Стейт на основе аккаунтов (баланс, nonce, стейк)
* Транзакции с учетом газа:
  * Перевод (Transfer)
  * Стейкинг / Анстейкинг (Stake / Unstake)
  * Отправка результата вычислений (Submit Compute Result)

### 🧠 **Слой Proof-of-Compute (PoC)**

* Встроенные типы: `ComputeTask`, `ComputeResult`
* Заголовок блока содержит `compute_root` (Merkle root результатов вычислений)
* Зарезервированные поля для **ZK-proofs** (будущая интеграция)
* Фундамент для выполнения и верификации задач на GPU-воркерах

### 🌐 **Сеть**

* Легковесный P2P протокол
* Автоматическая синхронизация и разрешение форков
* Сохранение пиров (`peers.json`)

---

## 🚀 Начало работы

Полная документация для разработчиков и валидаторов доступна в директории `/docs`:

* **Обзор архитектуры**
* **Запуск локальной ноды**
* **Стейкинг и Валидация**
* **Кошелек и Ключи (cpc-cli)**
* **GPU Воркеры и PoC Вычисления**
* **API / RPC Справочник**

Для локального запуска документации:

```bash
./start_docs.sh
```

Доступно по адресу: **[http://localhost:8008](http://localhost:8008)**

---

## 🛠 Структура Репозитория

```
computechain/
├── blockchain/      # L1 нода: консенсус, стейт, сеть
│   ├── core/        # Chain, мемпул, логика газа
│   ├── consensus/   # PoA движок (PQ-ready)
│   ├── p2p/         # Легковесный P2P протокол
│   └── storage/     # SQLite хранилище
├── protocol/        # Общие определения протокола
│   ├── types/       # Блоки, транзакции, PoC структуры
│   ├── crypto/      # Абстракция PQ подписей
│   └── config/      # Параметры сети и газа
├── cli/             # Инструмент кошелька cpc-cli
├── docs/            # Полная документация
└── scripts/         # Помощники для Devnet и E2E тесты
```

---

## 🧭 Roadmap (Верхнеуровневый)

### **Готово**

* Multi-Validator PoA Консенсус
* Динамический набор валидаторов
* Post-Quantum Архитектура подписей
* Модель Gas и рынок комиссий
* Фреймворк Proof-of-Compute (Stage 4)

### **В процессе**

* GPU Worker Runtime (Движок исполнения PoC)
* Оркестратор задач и Рынок вычислений
* Интеграция ZK-Proof для верификации вычислений

---

## 🧪 Разработка

Запуск юнит-тестов:

```bash
pytest computechain/blockchain/tests
```

Сценарий сквозного тестирования (E2E):

```bash
python3 scripts/e2e_battle.py
```

---

## 📄 Лицензия

**MIT License**

