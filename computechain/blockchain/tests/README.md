# Blockchain Tests

Набор юнит-тестов для проверки логики L1-ноды ComputeChain.

## 🚀 Запуск

Запускать из корня проекта:

```bash
python3 -m pytest computechain/blockchain/tests
```

## 📂 Описание тестов

| Файл | Что тестирует |
|---|---|
| **`test_core.py`** | Базовая логика стейта, переводы, проверка nonce и балансов. |
| **`test_signature.py`** | Проверка подписей транзакций (secp256k1) и защита от подмены полей. |
| **`test_block_signature.py`** | Валидация блоков, **Post-Quantum (PQ)** подписи валидаторов, проверка Time Drift. |
| **`test_poc.py`** | Логика **Proof-of-Compute**: транзакции `SUBMIT_RESULT`, проверка структур задач и воркеров. |

## ⚠️ Важно

- Тесты используют параметры **Gas & Fees** (Price: 1000, Limit: 21k/80k).
- Создаются временные SQLite базы данных, которые удаляются после выполнения.

