# ComputeChain Отчет по Аудиту Безопасности
## Комплексный анализ безопасности и оценка рисков

**Версия:** 1.2  
**Дата аудита:** 16 ноября 2025  
**Статус:** ⚠️ ВЫЯВЛЕНЫ КРИТИЧЕСКИЕ РИСКИ - ЗАПУСК MAINNET ЗАБЛОКИРОВАН  
**Аудитор:** Внутренняя проверка безопасности

---

## 🔍 Резюме

Этот документ представляет комплексный аудит безопасности архитектуры протокола ComputeChain. Аудит выявил **2 критических P0-блокера**, которые должны быть решены перед любым публичным запуском mainnet, а также несколько рисков высокого и среднего приоритета, требующих митигации.

### Ключевые находки

```python
AUDIT_SUMMARY = {
    "P0_BLOCKERS": 2,           # Должны быть исправлены до mainnet
    "P1_HIGH_PRIORITY": 2,      # Исправить в MVP
    "P2_MEDIUM_PRIORITY": 3,    # Решить в ходе разработки
    
    "overall_assessment": "Проект технически здравый, но требует архитектурных изменений",
    "recommendation": "Продолжить с MVP после устранения P0 рисков"
}
```

### Классификация рисков

- 🔴 **P0 BLOCKER**: Риск гибели цепи, требует немедленного исправления
- 🟠 **P1 HIGH**: Значительное влияние на безопасность/экономику
- 🟡 **P2 MEDIUM**: Важно, но не критично
- 🟢 **P3 LOW**: Улучшения nice-to-have

---

## 🔴 P0 BLOCKER #1: Модель доверия CUDA Binary

### Серьезность: КРИТИЧЕСКАЯ

### Описание

Реализация ByteLeap использует **CUDA binary со встроенным приватным ключом** для подписания результатов proof-of-compute. Это **НЕ является Trusted Execution Environment (TEE)** и представляет фундаментальный риск безопасности.

### Технический анализ

```python
# Текущая модель ByteLeap:
class CUDABinary:
    """CUDA binary со встроенным ключом"""
    
    EMBEDDED_PRIVATE_KEY = "..."  # ← УЯЗВИМОСТЬ
    
    def sign_result(self, seed: bytes, gpu_uuid: str, merkle_root: str):
        message = f"0x1|{seed}|{gpu_uuid}|{merkle_root}"
        signature = ecdsa_sign(self.EMBEDDED_PRIVATE_KEY, message)
        return signature

# Сценарий атаки:
# 1. Атакующий скачивает CUDA binary
# 2. Реверс-инжиниринг с IDA Pro / Ghidra
# 3. Извлечение приватного ключа из binary
# 4. Создание софтверного майнера (без GPU)
# 5. Отправка фейковых доказательств с валидными подписями
# 6. Получение наград без выполнения работы
```

### Timeline атаки

```
День 1-7:   Реверс-инжиниринг CUDA binary
            Инструменты: IDA Pro, Ghidra, Binary Ninja
            Сложность: СРЕДНЯЯ (требует экспертизы)

День 8-14:  Извлечение ключа из памяти/дизассемблера
            Методы: Статический анализ, динамическая отладка
            Сложность: СРЕДНЕ-ВЫСОКАЯ

День 15-21: Разработка софтверного эмулятора
            Создание фейкового майнера без GPU
            Сложность: НИЗКАЯ (когда ключ известен)

День 22:    Публикация в даркнет
            Эксплойт становится публичным
            
День 23+:   Компрометация сети
            1000+ атакующих используют эксплойт
            Цепь фактически мертва
```

### Почему это работает в Bittensor SN128

ByteLeap был разработан для **среды Bittensor subnet**:

✅ **Социальный слой**: Валидаторы знают майнеров, KYC-подобная модель  
✅ **Внешний консенсус**: Metagraph Bittensor TAO обеспечивает дополнительную безопасность  
✅ **Закрытая сеть**: Высокий барьер входа, ограниченные участники  
✅ **Система репутации**: Сообщество может вручную обнаружить подозрительное поведение  

### Почему это НЕ работает для публичного блокчейна

❌ **Анонимные участники**: Нет KYC, нет проверки личности  
❌ **Единственный слой безопасности**: Только криптография, нет социального контроля  
❌ **Высокий финансовый стимул**: Прямые награды за блоки = сильная мотивация к атаке  
❌ **Без разрешений**: Любой может присоединиться и эксплуатировать  

### Варианты митигации

#### Вариант A: Настоящий TEE (Intel SGX / AMD SEV)

```python
class TEEProtectedKey:
    """Ключ хранится в аппаратном анклаве"""
    
    def sign_in_enclave(self, message: bytes) -> bytes:
        # Ключ НИКОГДА не покидает анклав
        # Remote attestation доказывает подлинность
        return sgx_sign(message)

# Плюсы:
# ✅ Приватный ключ защищен аппаратно
# ✅ Remote attestation криптографически доказывает подлинное оборудование
# ✅ Невозможно извлечь ключ через дизассемблер

# Минусы:
# ❌ Требуются Intel SGX / AMD SEV CPU
# ❌ Не все GPU имеют TEE
# ❌ Сложная интеграция
# ❌ Возможны side-channel атаки (Spectre, Foreshadow)
```

#### Вариант B: Multi-Party Computation (MPC)

```python
class MPCSignature:
    """Подпись требует N сторон"""
    
    def __init__(self, threshold: int, total: int):
        self.threshold = threshold  # например, 2
        self.total = total          # например, 3
    
    def sign(self, message: bytes, partial_sigs: List[bytes]) -> bytes:
        # Требуется threshold подписей для создания валидной подписи
        if len(partial_sigs) < self.threshold:
            return None
        return combine_signatures(partial_sigs)

# Плюсы:
# ✅ Децентрализованное доверие (нет единого ключа)
# ✅ Компрометация 1 стороны не ломает систему

# Минусы:
# ❌ Очень сложная реализация
# ❌ Overhead производительности
# ❌ Сложность координации
```

#### Вариант C: Множественные независимые реализации

```python
class MultiImplementation:
    """3+ разных CUDA binary от разных команд"""
    
    implementations = [
        "cuda_impl_team_a",
        "cuda_impl_team_b", 
        "cuda_impl_team_c"
    ]
    
    def verify_challenge(self, results: List[Result]) -> bool:
        # Требуется согласие N-из-M
        if count_matching(results) >= 2:
            return True
        return False

# Плюсы:
# ✅ Компрометация 1 реализации не убивает сеть
# ✅ Разнообразие увеличивает безопасность
# ✅ Реалистично реализовать

# Минусы:
# ❌ Высокая стоимость разработки (3+ команды)
# ❌ Все реализации все еще уязвимы индивидуально
# ❌ Overhead координации
```

#### Вариант D: Отказ от привязки к оборудованию (РЕКОМЕНДУЕТСЯ ДЛЯ MVP)

```python
class AlgorithmicPoC:
    """Чистый алгоритмический proof-of-compute без доверия к оборудованию"""
    
    def verify_proof(self, commitment: Commitment, proof: Proof) -> bool:
        # Нет зависимости от аппаратно-специфичных подписей
        # Верификация математически через:
        # - Verifiable Delay Functions (VDF)
        # - Memory-hard функции (Argon2)
        # - Или простое умножение матриц с полной верификацией
        
        return cryptographic_verify(commitment, proof)

# Плюсы:
# ✅ Нет доверия к оборудованию/binary
# ✅ Простая реализация
# ✅ Криптографически надежно

# Минусы:
# ❌ Теряем GPU UUID анти-Sybil
# ❌ Нужна альтернативная Sybil-защита (экономический стейк)
```

### Рекомендация

**Для MVP:** Использовать **Вариант D** (алгоритмический PoC без привязки к оборудованию)

**Для будущего:** Исследовать **Вариант C** (мульти-реализация) + **Вариант A** (TEE для добровольцев)

### Влияние при неисправлении

```python
IMPACT_ASSESSMENT = {
    "probability": "ВЫСОКАЯ (50%+ в течение 6 месяцев после mainnet)",
    "impact": "КРИТИЧЕСКОЕ (полная компрометация сети)",
    "severity_score": 10/10,
    
    "consequences": [
        "Фейковые майнеры получают награды без GPU",
        "Честные майнеры не могут конкурировать",
        "Стоимость токена → 0",
        "Смерть сети",
        "Репутационный ущерб"
    ]
}
```

---

## 🔴 P0 BLOCKER #2: PoC ≠ Безопасность консенсуса

### Серьезность: КРИТИЧЕСКАЯ (Архитектурная/Маркетинговая)

### Описание

Текущая архитектура позиционирует ComputeChain как "блокчейн Proof-of-Compute", но в реальности **вычислительная мощность не влияет на безопасность консенсуса**. Только стейк определяет силу голоса.

### Техническая реальность

```python
# Текущая модель:
voting_power = stake_amount  # ← Только это имеет значение
compute_power = None          # ← Не влияет на консенсус

# Сценарий византийской атаки:
byzantine_validators = {
    "stake": "34% от общего",      # Может остановить/контролировать цепь
    "gpu_power": "5% сети"         # Почти нет GPU
}

# Результат:
can_halt_chain = True             # ✅ 34% > 33% BFT threshold
can_censor_tx = True
can_manipulate_rewards = True

# Тем временем:
honest_miners = {
    "gpu_power": "95% сети",
    "can_protect_chain": False     # ❌ GPU не важны для безопасности
}
```

### Сравнение моделей безопасности

**Bitcoin (Настоящий PoW):**
```
hash_power = voting_power = security
Больше hash power → Больше блоков → Контроль сети
Атака требует 51% hash power
```

**Текущий ComputeChain (PoS с compute-наградами):**
```
stake = voting_power = security
compute_power = только распределение наград
Атака требует 34% стейка (GPU не нужны)
GPU не защищает сеть
```

### Вводящий в заблуждение маркетинг

**Текущий нарратив:**
- "Блокчейн Proof-of-Compute"
- "GPU-майнинг обеспечивает безопасность сети"
- "Консенсус на основе вычислений"

**Реальность:**
- PoS блокчейн с compute-наградами
- Только стейк обеспечивает безопасность сети
- Вычисления определяют только распределение наград

### Варианты митигации

#### Вариант A: Настоящий консенсус Proof-of-Compute

```python
# Вычислительная мощность = сила голоса
def calculate_block_probability(miner: Miner) -> float:
    return miner.compute_power / total_network_compute

# Плюсы:
# ✅ Честный нарратив "PoC блокчейн"
# ✅ GPU реально защищает сеть
# ✅ Невозможно атаковать без GPU

# Минусы:
# ❌ Нет быстрого финалити (как Bitcoin)
# ❌ Проблема nothing-at-stake не решена
# ❌ Возможны longest-chain атаки
# ❌ Сложная реализация
```

#### Вариант B: Гибридный PoS + PoC (Рекомендуется для будущего)

```python
def calculate_voting_power(validator: Validator) -> float:
    stake_component = sqrt(validator.stake)
    compute_component = sqrt(validator.compute_power)
    
    return stake_component * compute_component

# Требования:
min_stake = 10_000 * 10**18  # 10k CPC
min_compute = 1  # Минимум 1 GPU

# Плюсы:
# ✅ Вычисления РЕАЛЬНО влияют на безопасность
# ✅ Сохраняем BFT быстрый финалити
# ✅ Атакующему нужны И стейк И GPU
# ✅ Более децентрализовано

# Минусы:
# ❌ Более сложный консенсус
# ❌ Сложнее реализовать
# ❌ Нужно балансировать веса stake vs compute
```

#### Вариант C: Честный ребрендинг (РЕКОМЕНДУЕТСЯ ДЛЯ MVP)

```
Просто называть как есть:
"ComputeChain: PoS блокчейн с верифицируемым compute-слоем"

Без вводящего в заблуждение маркетинга.
Compute-награды, не compute-безопасность.
```

### Рекомендация

**Для MVP:** Использовать **Вариант C** (честное позиционирование)

**Для будущего:** Реализовать **Вариант B** (гибридное голосование) когда сеть созреет

### Влияние при неисправлении

```python
IMPACT_ASSESSMENT = {
    "probability": "ОПРЕДЕЛЕННАЯ (если позиционировать как PoC)",
    "impact": "ВЫСОКОЕ (регуляторное, репутация, доверие пользователей)",
    "severity_score": 8/10,
    
    "consequences": [
        "Введение в заблуждение пользователей/инвесторов",
        "Регуляторный контроль (ложная реклама)",
        "Обратная реакция сообщества при обнаружении",
        "Потеря доверия",
        "Потенциальные юридические проблемы"
    ]
}
```

---

## 🟠 P1 HIGH PRIORITY #1: Гейминг Lease Revenue

### Серьезность: ВЫСОКАЯ

### Описание

В исходной модели скоринга ByteLeap 70% веса майнера приходит от "lease revenue". Атакующий может геймить это, создавая фейковые клиентские задания и платя самому себе.

### Сценарий атаки

```python
# Атакующий контролирует:
miner_address = "0xAAA..."
client_address = "0xBBB..."  # Тот же человек, другой кошелек

# Атака:
for _ in range(1000):
    # Отправить фейковое задание
    job = submit_job(
        client=client_address,
        miner=miner_address,
        payment=100 CPC  # Деньги идут самому себе
    )
    
    # "Выполнить" задание (легко, т.к. обе стороны под контролем)
    submit_fake_proof(job)
    
    # Чистая стоимость: Только комиссии транзакций (~0.01 CPC)
    # Выгода: Увеличенный вес → больше наград за блоки (10+ CPC)

# Результат через 1 месяц:
honest_miners_weight = 60%  # Делают реальную работу
attacker_weight = 40%       # Геймят систему
# Атакующий получает 40% всех наград за минимальную стоимость
```

### Митигация

**Решение: Механизм сжигания** ✅ РЕАЛИЗОВАНО

```python
# Когда клиент платит за compute-задание:
client_payment = 100 CPC

distribution = {
    "burned": 60 CPC,        # 60% уничтожается навсегда
    "miner": 25 CPC,         # 25% исполнителю
    "validators": 10 CPC,    # 10% верификаторам
    "treasury": 5 CPC        # 5% DAO
}

# Теперь гейминг дорогой:
attack_cost = 60 CPC (сожжено) * 1000 заданий = 60,000 CPC потеряно
attack_benefit = ~5,000 CPC увеличенных наград
# Чистый: -55,000 CPC убыток → Невыгодно
```

### Статус

✅ **ИСПРАВЛЕНО** в текущей спецификации

---

## 🟠 P1 HIGH PRIORITY #2: Слабость GPU UUID

### Серьезность: ВЫСОКАЯ

### Описание

GPU UUID **не является криптографическим идентификатором** и может быть изменен через модификацию прошивки или виртуализацию.

### Векторы атаки

**Вектор атаки #1: Прошивка VBIOS**
```bash
# Изменить прошивку GPU для смены UUID
$ nvidia-smi --save-bios original.rom
$ hex-editor original.rom  # Изменить байты серийного номера
$ nvidia-smi --flash-bios modified.rom

# Результат: Та же физическая GPU, другой UUID
# Можно зарегистрироваться как "новый" майнер
# Время: 30 минут
# Стоимость: $0
# Обнаружение: Очень сложно
```

**Вектор атаки #2: Виртуализация (vGPU/MIG)**
```python
# NVIDIA MIG (Multi-Instance GPU):
physical_gpu = "1x RTX A100"

# Можно разделить на:
virtual_gpus = [
    "GPU-uuid-1",  # MIG instance 1
    "GPU-uuid-2",  # MIG instance 2
    "GPU-uuid-3",  # MIG instance 3
    # ... до 7 инстансов
]

# Каждый выглядит как отдельная GPU с уникальным UUID
# Но все на одном физическом оборудовании
```

**Вектор атаки #3: Облачные провайдеры**
```python
# AWS/Azure могут:
# 1. Создавать множественные VM инстансы
# 2. Каждый с "уникальным" GPU UUID
# 3. Но разделяющие время физической GPU
# 4. Валидаторы не могут это обнаружить
```

### Митигация: Многослойная защита

```python
class GPUVerification:
    """Многослойные проверки подлинности GPU"""
    
    def verify_gpu(self, miner: Miner) -> bool:
        # Слой 1: Уникальность UUID (базовый)
        if not self.check_uuid_unique(miner.gpu_uuid):
            return False
        
        # Слой 2: Обнаружение виртуализации
        if self.detect_virtualization(miner):
            return False
        
        # Слой 3: Timing fingerprinting
        profile = self.collect_timing_profile(miner)
        if self.is_anomalous(profile):
            return False
        
        # Слой 4: Экономический стейк
        if miner.stake < 10_000 * 10**18:
            return False
        
        # Слой 5: Постепенное доверие
        if miner.challenge_count < 1000:
            # Низкое доверие, высокая частота верификации
            return self.verify_frequently(miner)
        
        return True
```

### Рекомендация

Реализовать стратегию **многослойной защиты** (см. документ исследования GPU безопасности для деталей).

### Статус

⚠️ **ЧАСТИЧНО РЕШЕНО** - Базовая проверка UUID существует, нужны дополнительные слои

---

## 🟡 P2 MEDIUM #1: Ограничения производительности Python

### Серьезность: СРЕДНЯЯ (Влияние на UX)

### Описание

Реализация на Python реалистично достигнет только **50-100 TPS**, а не рекламируемые 1000+ TPS.

### Анализ производительности

```python
# Реалистичные бенчмарки Python:

ecdsa_verify = 0.5ms на подпись
# Для 1000 TPS: 1000 * 0.5ms = 500ms только на подписи

merkle_verify = 2ms на доказательство
# Для 100 доказательств/блок: 200ms

database_writes = 50ms на блок

json_serialization = 10ms на блок

# Всего на блок: 500 + 200 + 50 + 10 = 760ms
# Это ТОЛЬКО верификация, без P2P распространения!

# Реалистичный TPS: 50-100 TPS
```

### Митигация

**Вариант A: Признать ограничение**
```
MVP документация четко указывает:
"50-100 TPS в Python testnet"
"Production mainnet будет использовать Cosmos SDK/Substrate для 1000+ TPS"
```

**Вариант B: Переписывание на Rust**
```python
MIGRATION_PLAN = {
    "фаза_1": "Python MVP (50-100 TPS)",
    "фаза_2": "Миграция на Cosmos SDK (1000-5000 TPS)",
    "timeline": "6-9 месяцев после MVP"
}
```

### Статус

⚠️ **ПРИЗНАНО** - Ясно в документации MVP

---

## 🟡 P2 MEDIUM #2: Риск расширения scope

### Серьезность: СРЕДНЯЯ (Влияние на timeline)

### Описание

Исходная оценка Фазы 1 в 7,200 строк кода **недооценена**. С логированием, тестированием, обработкой ошибок, реальное число будет 15,000-20,000 строк.

### Рекомендация

**Сократить scope MVP на 50%:**

```python
MVP_MINIMAL = {
    "include": [
        "Базовый блокчейн (блоки, цепь, состояние)",
        "CPC переводы токенов",
        "Простые compute-челленджи",
        "1-3 валидатора PoA",
        "RPC API (минимальный)",
        "CLI кошелек"
    ],
    
    "exclude": [
        "Контракты маркетплейса",
        "DeFi интеграции",
        "Смарт-контракты / VM",
        "Продвинутый P2P (использовать простой TCP)",
        "Block explorer UI",
        "DAO управление"
    ],
    
    "estimated_code": "3,000-5,000 строк",
    "timeline": "2-3 месяца (не 4)"
}
```

### Статус

✅ **РЕШЕНО** в спецификации v1.2

---

## 🟡 P2 MEDIUM #3: Конфигурация Slashing

### Серьезность: СРЕДНЯЯ

### Описание

Исходный slashing использовал фиксированные суммы CPC (например, 50,000 CPC за двойное подписание). Если цена токена вырастет в 100x, это станет непомерно дорогим и отпугнет валидаторов.

### Митигация

```python
# Изменено на процентную основу:
SLASHING_CONDITIONS = {
    "double_signing": "50% от стейка",
    "invalid_proof": "20% от стейка",
    "downtime": "5% от стейка",
    "censorship": "10% от стейка"
}

# Теперь масштабируется с размером стейка валидатора
# Не зависит от изменений цены токена
```

### Статус

✅ **ИСПРАВЛЕНО** в текущей спецификации

---

## 📊 Матрица приоритетов рисков

### Сводная таблица

| Риск | Приоритет | Вероятность | Влияние | Статус |
|------|----------|-------------|--------|--------|
| Доверие CUDA binary | 🔴 P0 | ВЫСОКАЯ | КРИТИЧЕСКОЕ | ⚠️ БЛОКЕР |
| PoC консенсус несоответствие | 🔴 P0 | ОПРЕДЕЛЕННАЯ | ВЫСОКОЕ | ⚠️ БЛОКЕР |
| Гейминг Lease revenue | 🟠 P1 | ВЫСОКАЯ | ВЫСОКОЕ | ✅ ИСПРАВЛЕНО |
| Слабость GPU UUID | 🟠 P1 | СРЕДНЯЯ | СРЕДНЕЕ | ⏳ ЧАСТИЧНО |
| Производительность Python | 🟡 P2 | ОПРЕДЕЛЕННАЯ | СРЕДНЕЕ | ✅ ПРИЗНАНО |
| Расширение scope | 🟡 P2 | ВЫСОКАЯ | СРЕДНЕЕ | ✅ РЕШЕНО |
| Конфиг slashing | 🟡 P2 | НИЗКАЯ | НИЗКОЕ | ✅ ИСПРАВЛЕНО |

---

## ✅ Рекомендации

### Перед написанием кода

1. ⚠️ **КРИТИЧНО**: Решить механизм compute-доказательств
   - Отказаться от привязки GPU UUID ИЛИ
   - Реализовать надежную анти-виртуализацию ИЛИ
   - Подождать интеграции TEE

2. ⚠️ **КРИТИЧНО**: Исправить маркетинговый нарратив
   - Называть "PoS с compute-наградами" ИЛИ
   - Реализовать гибридное голосование (stake + compute)

3. ✅ Сохранить механизм сжигания (уже сделано)

### Разработка MVP (Фаза 1)

```python
MVP_PRIORITIES = {
    "P0_must_have": [
        "Алгоритмический PoC (без привязки к оборудованию)",
        "Честное позиционирование (не 'PoC консенсус')",
        "Базовый экономический стейк (10k CPC на GPU)"
    ],
    
    "P1_should_have": [
        "Белый список GPU (4090/5090)",
        "Базовые проверки виртуализации",
        "Timing fingerprinting (простой)"
    ],
    
    "P2_nice_to_have": [
        "ML обнаружение аномалий",
        "Продвинутые PCI проверки",
        "Поведенческое профилирование"
    ]
}
```

### После MVP (Фаза 2-3)

- Стратегия мульти-реализации
- Исследование TEE (SGX/SEV)
- Гибридное PoS+PoC голосование
- Продвинутые анти-Sybil слои

---

## 🎯 Финальная оценка

```python
SECURITY_ASSESSMENT = {
    "idea_quality": "9/10 - Сильная концепция",
    "current_architecture": "6/10 - Имеет P0 блокеры",
    "after_fixes": "8/10 - Солидный фундамент",
    
    "can_launch_mainnet_now": False,
    "can_launch_after_fixes": True,
    
    "recommendation": "Исправить P0 блокеры → MVP testnet → итерация → mainnet"
}
```

### Может ли проект быть успешным?

**ДА, но требуется:**

1. Честная оценка модели доверия (никакого CUDA binary как якорь безопасности)
2. Четкое позиционирование (PoS + compute, не чистый PoC)
3. Реалистичный scope MVP (3-4 месяца, не переобещать)
4. Многослойный подход к безопасности (экономический + криптографический + поведенческий)

### Timeline до безопасного запуска

```
Месяц 1-2:   Исправить P0 архитектурные проблемы
Месяц 3-5:   MVP testnet с честным позиционированием
Месяц 6-8:   Bug bounty + улучшения безопасности
Месяц 9-12:  Миграция на production стек (Cosmos/Substrate)
Месяц 13-14: Запуск Mainnet с 50+ валидаторами
```

---

## 📞 Контакт команды безопасности

Для проблем безопасности или отчетов об уязвимостях:

- **Email**: security@computechain.io
- **Bug Bounty**: (TBA после MVP)
- **PGP Key**: (TBA)

---

**Версия 1.2** | Отчет по аудиту безопасности  
**Последнее обновление:** 16 ноября 2025

*Этот аудит будет обновляться по мере развития проекта и выявления новых рисков.*

---

## Приложение A: Модель угроз

```python
THREAT_ACTORS = {
    "script_kiddie": {
        "skill": "НИЗКИЙ",
        "resources": "НИЗКИЕ",
        "motivation": "Веселье/обучение",
        "can_exploit": ["Ни один из наших рисков"]
    },
    
    "motivated_individual": {
        "skill": "СРЕДНИЙ",
        "resources": "СРЕДНИЕ ($1k-10k)",
        "motivation": "Финансовая выгода",
        "can_exploit": ["Слабость GPU UUID", "Прошивка VBIOS"]
    },
    
    "professional_attacker": {
        "skill": "ВЫСОКИЙ",
        "resources": "ВЫСОКИЕ ($100k+)",
        "motivation": "Финансовая выгода",
        "can_exploit": ["Извлечение CUDA binary", "Все GPU атаки", "Экономический гейминг"]
    },
    
    "nation_state": {
        "skill": "ОЧЕНЬ ВЫСОКИЙ",
        "resources": "НЕОГРАНИЧЕННЫЕ",
        "motivation": "Нарушение сети",
        "can_exploit": ["Все", "TEE side-channels", "Атаки консенсуса"]
    }
}

DEFENSE_TARGETS = {
    "mvp": "Защита от мотивированных индивидуалов",
    "mainnet": "Защита от профессиональных атакующих",
    "long_term": "Повысить планку для nation-state атак"
}
```

---

**Конец отчета по аудиту безопасности**

